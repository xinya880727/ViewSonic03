// 遊戲設定檔
// 您可以在這裡修改圖片、文字、遮罩形狀與遊戲參數

const GameConfig = {
    // ==========================================
    // 1. 介面與外觀 (UI & Assets)
    // ==========================================
    title: "衣櫃外的腳步聲停了......",
    description: "殺人魔的房間藏著秘密。請橫持裝置並轉動角度，找出隱藏的密碼",
    startButtonText: "屏住呼吸偷看",
    
    // 房間背景圖片 (被偷看的那個房間)
    backgroundImage: 'picture.png',

    // ==========================================
    // 2. 遮罩設定 (Mask) - 決定洞的形狀與大小
    // ==========================================
    mask: {
        // [關鍵設定] 遮罩模式
        // 'svg'   = 使用下方的 shapes 語法畫出幾何形狀
        // 'image' = 使用下方的 imageSrc 圖片 (必須是 PNG 透明圖)
        mode: 'image', 

        // [通用] 縮放比例 (對 SVG 和 圖片 都有效)
        // 1.0 = 原始大小, 1.5 = 變大, 0.5 = 變小
        scale: 1.0,

        // --- 設定 A: 如果 mode 是 'image' ---
        // 請準備一張 PNG 圖片：
        // 1. 中間的「洞」必須是「透明像素」(Transparency)。
        // 2. 四周必須是「不透明」的顏色（例如黑色、木紋、金屬）。
        // 3. 圖片尺寸建議大於螢幕 (例如 1920x1080)，以免露出邊緣。
        imageSrc: 'hover03.png', 

        // --- 設定 B: 如果 mode 是 'svg' ---
        // 黑色(fill="black") 會變成透明的洞，白色是不透明的牆。
        shapes: `
            <circle cx="50" cy="40" r="15" fill="black" />
            <path d="M50 40 L65 90 L35 90 Z" fill="black" />
        `
    },

    // ==========================================
    // 3. 線索紙條 (The Clue)
    // ==========================================
    clue: {
        show: false,
        text: "",
        code: "9527",
        
        // 紙條位置 (top: 垂直, left: 水平)
        top: "60%",         
        left: "20%",
        
        rotation: -5
    },

    // ==========================================
    // 4. 遊戲手感參數 (Gameplay)
    // ==========================================
    gameplay: {
        // [新增修正] 靈敏度倍率 (動作放大器)
        // 預設 2.0，代表手轉 1 度，畫面轉 2 度。
        // 這能解決「看不到邊緣」的問題，讓您只需輕微轉動就能看到全景。
        sensitivity: 2.0,

        maxMoveX: 50,       // 左右視野移動範圍 (vw)
        maxMoveY: 50,       // 上下視野移動範圍 (vh)
        smoothFactor: 0.02,  // 移動平滑度 (數值越小越慢/越飄)
        limitAngle: 30       // 手機傾斜靈敏度
    }

};




